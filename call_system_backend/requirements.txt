# Web framework
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# Database – PostgreSQL
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
alembic>=1.13.0

# Task queue
celery[redis]>=5.3.0
redis>=5.0.0

# Speech-to-text
openai-whisper

# Fine-tuned LLM inference (LoRA on facebook/opt-125m)
torch>=2.0.0
transformers>=5.0.0
peft>=0.19.1          # REQUIRED: loads your .safetensors LoRA adapter
accelerate>=0.30.0
sentencepiece

# Keep ONLY if you use embedding/semantic similarity search
# sentence-transformers

# DEV / TRAINING ONLY – do not deploy to production
# trl>=1.3.0